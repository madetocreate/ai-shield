# AI Shield Environment Variables
# 
# Kopiere diese Datei nach backend.env (Standard: ~/Documents/Backend-Secrets/backend.env)
# oder setze BACKEND_ENV_PATH auf den gewünschten Pfad.
#
# WICHTIG: Diese Datei enthält Platzhalterwerte. Setze sichere Passwörter für Production!

# ============================================================================
# REQUIRED VARIABLES (müssen gesetzt werden, sonst startet docker-compose nicht)
# ============================================================================

# PostgreSQL für LiteLLM Gateway
POSTGRES_PASSWORD=change-me-postgres-password

# PostgreSQL für Langfuse
LANGFUSE_DB_PASSWORD=change-me-langfuse-db-password

# Langfuse Authentication
LANGFUSE_SECRET=change-me-langfuse-secret-min-32-chars
LANGFUSE_URL=http://localhost:3000

# Grafana Admin Credentials
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me-grafana-password

# ============================================================================
# OPTIONAL: Port Configuration (Host Ports)
# ============================================================================
# Standard-Ports sind bereits in docker-compose.yml definiert.
# Überschreibe diese nur, wenn Port-Kollisionen auftreten.

# PostgreSQL Ports (HOST_PORT Variablen wie in docker-compose.yml)
POSTGRES_HOST_PORT=5435
LANGFUSE_DB_PORT=5436

# Redis
REDIS_PORT=6379

# Presidio Services
PRESIDIO_ANALYZER_HOST_PORT=5001
PRESIDIO_ANONYMIZER_HOST_PORT=5002

# OpenTelemetry Collector
OTEL_GRPC_PORT=4317
OTEL_HTTP_PORT=4318

# AI Shield Services
AI_SHIELD_HOST_PORT=4050
CONTROL_PLANE_HOST_PORT=4051

# Observability
LANGFUSE_PORT=3000
GRAFANA_PORT=3001
PROMETHEUS_PORT=9090

# Jaeger Tracing
# WICHTIG: JAEGER_HTTP_PORT sollte NICHT 16686 sein (das ist der UI-Port)
# Standard: 14318 (um Kollision mit OTEL_HTTP_PORT zu vermeiden)
JAEGER_UI_PORT=16686
JAEGER_HTTP_PORT=14318

# Nginx Reverse Proxy
NGINX_PORT=80
NGINX_HTTPS_PORT=443

# ============================================================================
# OPTIONAL: Backend Environment File Path
# ============================================================================
# Pfad zur backend.env Datei, die API-Keys und weitere Secrets enthält.
# Standard: ~/Documents/Backend-Secrets/backend.env
# BACKEND_ENV_PATH=/path/to/your/backend.env

# ============================================================================
# OPTIONAL: Gateway & Control Plane Secrets (aus backend.env)
# ============================================================================
# Diese Variablen werden aus backend.env geladen (via env_file in docker-compose.yml)
# OPENAI_API_KEY=
# LITELLM_MASTER_KEY=
# CONTROL_PLANE_ADMIN_KEY=
# LANGFUSE_PUBLIC_KEY=
# LANGFUSE_SECRET_KEY=

# ============================================================================
# HINWEISE
# ============================================================================
# - Diese Datei wird NICHT direkt von docker-compose geladen.
# - docker-compose lädt Variablen aus der Shell-Umgebung oder aus backend.env (via env_file).
# - Für lokale Entwicklung: Exportiere Variablen in deiner Shell oder setze BACKEND_ENV_PATH.
# - Für Production: Nutze Docker Secrets, K8s ConfigMaps oder env_file mit sicherem Pfad.
