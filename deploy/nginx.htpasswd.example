# Beispiel htpasswd Datei
# 
# Erstelle die echte Datei mit:
#   htpasswd -c deploy/nginx.htpasswd <username>
#
# Oder mit Docker:
#   docker run --rm httpd:2.4-alpine htpasswd -nbB admin 'your-strong-password' > deploy/nginx.htpasswd
#
# WICHTIG: deploy/nginx.htpasswd ist in .gitignore und wird NICHT committet!
#
# Format (bcrypt):
# username:$2y$05$hashedpassword...
#
# Beispiel (NICHT für Production verwenden!):
# admin:$2y$05$example.hash.here...
